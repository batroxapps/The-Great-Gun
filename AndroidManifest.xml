<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="be.batrox.thegreatgun"
      android:versionCode="1"
      android:versionName="1.0">
    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher">
        <activity android:name=".MainActivity"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
		<activity android:name=".game.PlayActivity"
				  android:label="@string/app_name">
		</activity>
		<activity android:name=".game.shop.ShopActivity"
				  android:label="@string/app_name">
		</activity>
		<activity android:name=".tutorial.TutorialActivity"
				  android:label="@string/app_name">
		</activity>
		<activity android:name=".options.OptionsActivity"
				  android:label="@string/app_name">
		</activity>
		<activity android:name=".credits.CreditsActivity"
				  android:label="@string/app_name">
		</activity>
    </application>
</manifest>
